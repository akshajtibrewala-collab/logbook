{
  "name": "pilot-logbook",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c yellow,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "test": "npm test -w server && npm test -w client",
    "build": "npm run build -w client",
    "build:vercel": "npm run migrate -w server && npm run build -w client",
    "db:setup": "npm run db:setup -w server",
    "db:copy-local:prod": "npm run db:copy-local:prod -w server",
    "deploy": "npx vercel --prod",
    "db:backup": "npm run db:backup -w server",
    "db:backup:prod": "npm run db:backup:prod -w server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "type": "module",
  "engines": {
    "node": "22.x"
  }
}
